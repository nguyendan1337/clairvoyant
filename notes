etf df columns
['Symbol', 'Name', '', 'Price', 'Change', 'Change %', 'Volume', '50 DayAverage', '200 DayAverage', '3 Month Return', 'YTD Return', '52 WkChange %', '52 Wk Range']

stock df columns
['Symbol', 'Name', '', 'Price', 'Change', 'Change %', 'Volume', 'Avg Vol (3M)', 'Market Cap', 'P/E Ratio(TTM)', '52 WkChange %', '52 Wk Range']

You might also consider dynamic weighting, e.g., adjusting weights based on market conditions:
Bull markets → more momentum
Bear markets → more quality

2️⃣ Ignore index.html changes on other branches
After creating a branch:
# Example: create a new feature branch
git checkout -b feature-branch

# Tell Git to ignore local changes to index.html on this branch
git update-index --assume-unchanged index.html

Features to add:
- wdc has fallen in the rankings cuz others have higher quality and better value,
- if i want recognizable names with good momentum, add market cap to append qvm data, then use market cap in quality score?
- upgrade gemini prompt for more consistent results
    - sometimes explains its steps
    - max 2 or 3? stocks per sector
    - downweight recent performers due to recent news (energy being high due to iran war but collapses after ceasefire))
    - consistently outputs stock specific blurb in summaries instead of vague general speak sometimes
- polish
- same file, classes, abstraction, different object types for stock and etf, different configs